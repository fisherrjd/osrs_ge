[project]
name = "osrs_ge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.32.4",
    "sqlmodel>=0.0.24",
]

[tool.setuptools]
packages = []
